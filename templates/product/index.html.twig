{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>
    {{ form_start(form) }}
    <div class="form-row align-items-end" >
        <div class="col">
            {{ form_row(form.name) }}
        </div>
        <div class="col">
            <div class="form-group">
                <button type="submit" class="btn btn-success">Rechercher</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
    {# <table class="table"> #}
    {# <thead> #}
    {# <tr> #}
    {# <th>Id</th> #}
    {# <th>Name</th> #}
    {# <th>Description</th> #}
    {# <th>Src</th> #}
    {# <td>Categorie</td> #}
    {# <th>actions</th> #}
    {# </tr> #}
    {# </thead> #}
    {# <tbody> #}
    <div class="row">
        {% for product in products %}
            <div class="card col-3" style="width: 18rem;">
                <img class="card-img-top" src="{{ asset('img/' ~ product.src) }}" alt="product image ">
                <div class="card-body">
                    <h5 class="card-title">{{ product.name }}</h5>
                    <p class="card-text">{{ product.description }}</p>
                    <a href="{{ path('product_show', {'id': product.id}) }}" class="btn btn-sm btn-dark">show</a>
                    <a href="{{ path('product_edit', {'id': product.id}) }}" class="btn btn-sm btn-warning">edit</a>
                    {{ include('product/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="col-1"></div>
        {% endfor %}
    </div>


    {# <tr> #}
    {# <td>{{ product.id }}</td> #}
    {# <td>{{ product.name }}</td> #}
    {# <td>{{ product.description }}</td> #}
    {# <td><img src="{{ asset('img/' ~ product.src) }}" alt="Product image"></td> #}
    {# <td>{{ product.categorie.name }}</td> #}
    {# <td> #}
    {# <a href="{{ path('product_show', {'id': product.id}) }}">show</a> #}
    {# <a href="{{ path('product_edit', {'id': product.id}) }}">edit</a> #}
    {# </td> #}
    {# </tr> #}
    {# {% else %} #}
    {# <tr> #}
    {# <td colspan="5">no records found</td> #}
    {# </tr> #}
    {# {% endfor %} #}
    {# </tbody> #}
    {# </table> #}

    <a href="{{ path('product_new') }}" class="btn btn-primary mt-3">Create new</a>
{% endblock %}
